[package]
name = "delocaliser-rewrite"
version = "0.1.0"
edition = "2021"
description = "Content rewriting and inpainting for delocaliser"
license = "GPL-3.0"

[dependencies]
delocaliser-core = { path = "../core" }
