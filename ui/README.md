# UI Module

This module contains the user interface components for delocaliser.

## Planned Components

- **CLI**: Command-line interface for batch processing
- **Desktop app**: Cross-platform GUI with drag-and-drop support
- **Progress reporting**: Real-time processing status and metrics display

## Status

This module is planned for M8 (CLI + Desktop UI).
